{
  "mcpServers": {
    "project-hub-mcp": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/run-server",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/project-hub-server/run.py"],
      "env": {
        "CLAUDE_PLUGIN_ROOT": "${HOME}/.claude/plugins/marketplaces/project-hub"
      }
    }
  }
}
